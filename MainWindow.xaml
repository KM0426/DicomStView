<Window x:Class="DicomStView.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DicomStView"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Button Grid.Row="0"
                Width="190"
                HorizontalAlignment="Left"
                Padding="12,6"
                Content="フォルダを選択"
                Click="SelectFolderButton_Click" />

        <TextBlock x:Name="SelectedFolderTextBlock"
                   Grid.Row="1"
                   Margin="0,12,0,8"
                   TextWrapping="Wrap"
                   Text="フォルダ未選択" />

        <ListBox x:Name="DicomFilesListBox" Grid.Row="2" MouseDoubleClick="DicomFilesListBox_MouseDoubleClick">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Padding="8" Margin="0,0,0,8" BorderBrush="#DDDDDD" BorderThickness="1" CornerRadius="4">
                        <StackPanel>
                            <TextBlock Text="{Binding StudyInstanceUid, StringFormat=StudyInstanceUID: {0}}"
                                       FontWeight="SemiBold"
                                       TextWrapping="Wrap" />
                            <TextBlock Margin="0,4,0,0"
                                       Text="{Binding ModalitiesDisplay, StringFormat=Modality: {0}}"
                                       TextWrapping="Wrap" />
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
